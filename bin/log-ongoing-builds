#!/usr/bin/env bash

cwd="$(cd "$(dirname "$0")" && pwd)"

tmux new-session -s logs
tmux select-layout -t logs even-horizontal
for id in $("$cwd/ongoing-builds")
do
    tmux split-window -t logs "echo 'Streaming $id'; gcloud builds log --stream $id; bash"
done
