#!/usr/bin/env bash

app_name=$1

if [ -z "$app_name" ]
then
  echo "Usage: show-certificate APP_NAME"
  echo
  exit 1
fi

openssl x509 -in <(show-credential istio-system "tls-$app_name" tls.crt) -text -noout
