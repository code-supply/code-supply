apiVersion: apps/v1
kind: Deployment
metadata:
  name: site-operator
  namespace: operators
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/proxyCPU: 50m
        sidecar.istio.io/proxyCPULimit: 1000m
